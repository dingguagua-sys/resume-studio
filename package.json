{
  "name": "resume-studio",
  "private": true,
  "version": "1.0.0",
  "description": "Multi-user online resume builder — React + Node + MongoDB",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18.18"
  }
}
