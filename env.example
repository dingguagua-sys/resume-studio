# 复制为项目根目录的 .env（server 会自动从仓库根或 server/ 查找）
#
# 本机 MongoDB（需先启动服务，例如 brew services start mongodb-community）：
# MONGODB_URI=mongodb://127.0.0.1:27017/resume-studio
#
# 或使用 MongoDB Atlas 云数据库：
MONGODB_URI=mongodb+srv://USER:PASS@cluster0.xxxxx.mongodb.net/resume-studio?retryWrites=true&w=majority
JWT_SECRET=change-me-to-a-long-random-string
# 本地开发；上线 Vercel 填生产地址，多个用英文逗号分隔，例如：
# CLIENT_ORIGIN=https://resume-xxx.vercel.app,https://你的域名.com
CLIENT_ORIGIN=http://localhost:5173
